<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Cyan circle shape -->
    <item>
        <shape android:shape="oval">
            <size
                android:width="48dp"
                android:height="48dp" />
            <solid android:color="#FFFFFF" /> <!-- Cyan color -->
        </shape>
    </item>

    <!-- Check icon -->
    <item
        android:drawable="@drawable/check_icon"
        android:gravity="center" />
</layer-list>